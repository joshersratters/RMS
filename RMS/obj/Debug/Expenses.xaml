<Page
    x:Class="RMS.Expenses"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RMS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="0,0,0,0">
            <TextBlock Text="RMS" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0" Foreground="#FF4EA234"/>
            <TextBlock Text="expenses" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--Content Grid-->
        <Grid Grid.Row="4" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.5*" x:Name="Type"/>
                <RowDefinition Height="2*" x:Name="Capture"/>
                <RowDefinition Height="2*" x:Name="Preview"/>
                <RowDefinition Height="0.5*" x:Name="Submit"/>
            </Grid.RowDefinitions>
            <ComboBox Grid.Row="0" x:Name="expenseType"/>
            <CaptureElement x:ConnectionId='1'                                Grid.Row="1" x:Name="capturePreview" Margin="0,0,0,0"/>
            <Image x:ConnectionId='2'                                    Grid.Row="2" x:Name="imagePreview"/>
            <Button Grid.Row="3" x:Name="expenseSubmit" Content="Submit" HorizontalAlignment="Center"/>
            <Button x:ConnectionId='3'                                     Grid.Row="3" x:Name="cameraStart" Content="Camera Start" HorizontalAlignment="Left"/>
            <Button x:ConnectionId='4' Grid.Row="3" x:Name="cameraStop"                                    Content="Camera Stop" HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</Page>

